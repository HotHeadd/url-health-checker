services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: urlchecker
      POSTGRES_PASSWORD: computer
      POSTGRES_DB: urlchecker
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
  